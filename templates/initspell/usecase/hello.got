package usecase

import "fmt"

//SayHello to the provided name. In a real scenario, you would add here your command's business rules
// and decompose it in other Use Cases if necessary
func SayHello(args []string) error {
	fmt.Printf("Hello %s!\n", args[0])
	return nil
}
